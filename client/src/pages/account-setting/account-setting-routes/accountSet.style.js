import styled from "styled-components";
import { borderColor } from "../../../utils/style.utils";

export const AccountSetContainer = styled.div`
  width: 1000px;
  height: 600px;
  margin: auto;
  border: 1px solid ${borderColor};
  border-radius: 10px;
  margin-top: 20px;
  display: flex;
`;
